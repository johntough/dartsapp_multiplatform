extends layout

block layout-content
    div.View.UserView
        h1.Banner Howdy, #{userProfile.nickname}!
        div.Message
            div.Title
                h3 BMC
                h1 Teammate Profile
            h3 Player Name: #{displayName}
            br
            img(src=picture)
        div.NavButtons
            a(href="/logout")
                div.NavButton Log out